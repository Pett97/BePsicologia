<section>
  <div class="row">
    <form action="<?= route("schedule.update", ["id" => $fixedSchedule->id]) ?>" method="POST" class="col s12">
      <div class="row">
        <div class="input-field col s12 m3 l6">
          <input type="hidden" name="_method" value="PUT">
          <select name="fixedSchedule[psychologist_id]">
            <?php if ($isMaster) : ?>
              <?php foreach ($users as $user) : ?>
                <option value="<?= $user->id ?>"><?= $user->name ?></option>
              <?php endforeach; ?>
            <?php else : ?>
              <option value="<?= $user->id ?>"><?= $user->name ?></option>
            <?php endif; ?>
          </select>
          <label>Selecionar O Responsável</label>
        </div>
        <div class="input-field col s12 m12 l6">
          <input type="hidden" name="_method" value="PUT">
          <input value="<?= $fixedSchedule->day_of_week ?>" placeholder="Novo Dia Semana" id="newDayOFWeek" type="number" class="validate" name="fixedSchedule[day_of_week]" required>
          <label for="newDayOFWeek">Dia Semana</label>
        </div>
        <div class="input-field col s12 m12 l6">
          <input type="hidden" name="_method" value="PUT">
          <input value="<?= $fixedSchedule->start_time ?>" placeholder="Novo Horário de Inicio" id="newStartTime" type="text" class="validate" name="fixedSchedule[start_time]" required>
          <label for="newStartTime">Horario inicio</label>
        </div>
        <div class="input-field col s12 m12 l6">
          <input type="hidden" name="_method" value="PUT">
          <input value="<?= $fixedSchedule->end_time ?>" placeholder="Novo Horário de Fim" id="newDayOFWeek" type="text" class="validate" name="fixedSchedule[end_time]" required>
          <label for="newEndTime">Horario FIm</label>
        </div>
      </div>
      <button class="waves-effect waves-light btn blue" type="submit">Salvar</button>
    </form>
  </div>
</section>